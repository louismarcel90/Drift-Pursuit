# Global Verification Strategy

## Purpose

DRIFT PURSUIT GRID uses global verification to prove that the system behaves deterministically across scenarios.

This strategy validates more than individual units.

It verifies the full chain:

```text
scenario → input log → simulation → replay → debrief → evidence pack
```
