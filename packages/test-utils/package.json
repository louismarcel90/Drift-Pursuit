{
  "name": "@drift-pursuit-grid/test-utils",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --watch --config vitest.config.ts",
    "clean": "rimraf dist coverage"
  },
  "dependencies": {
    "@drift-pursuit-grid/contracts": "workspace:*",
    "@drift-pursuit-grid/debrief-engine": "workspace:*",
    "@drift-pursuit-grid/replay-engine": "workspace:*",
    "@drift-pursuit-grid/scenario-kit": "workspace:*",
    "@drift-pursuit-grid/simulation-core": "workspace:*",
    "@drift-pursuit-grid/state-store": "workspace:*",
    "@drift-pursuit-grid/evidence-engine": "workspace:*"
  },
  "devDependencies": {
    "@drift-pursuit-grid/shared-config": "workspace:*"
  }
}
